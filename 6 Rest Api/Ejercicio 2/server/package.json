{
  "name": "hotel-api",
  "version": "1.0.0",
  "description": "Hotel API",
  "main": "index.js",
  "scripts": {
    "mock-server": "json-server --routes ./config/routes.json --watch mock-data/db.json --middlewares ./config/character.middleware.js --no-delete-cascade --port 3000"
  },
  "author": "Lemoncode",
  "license": "MIT",
  "dependencies": {
    "json-server": "^0.16.1"
  }
}
